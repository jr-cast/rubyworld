<div
  id="sign-in-background"
  class="col-lg-6 d-flex flex-column justify-content-center align-items-center px-4 w-100" 
  style="height: 100vh;"
>
  <div
    id="card-signin" 
    class="card shadow p-4"
  >
    <div class="card-header">
      <h1 class="text-center fw-bold">Sign in 💎</h1>
    </div>
    <div class="card-body">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group">
          <%= f.label :email, class: 'mb-2' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-md mb-2" %>
        </div>
        <div class="form-group">
          <%= f.label :password, class: "mb-2" %>
          <%= f.password_field :password, autocomplete: "current-password", class: "form-control form-control-md" %>
        </div>
        <% if devise_mapping.rememberable? %>
          <div class="form-group mb-2 mt-2">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me, class: 'mb-2' %>
          </div>
        <% end %>
        <div class="actions">
          <%= f.submit "Log in", class: "btn btn-danger btn-lg mb-3" %>
        </div>
      <% end %>
      <%= render "users/shared/links" %>
    </div>
  </div>
</div>
      